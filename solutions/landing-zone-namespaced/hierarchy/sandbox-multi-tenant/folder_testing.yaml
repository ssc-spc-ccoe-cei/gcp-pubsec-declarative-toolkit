apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
kind: Folder
metadata:
  name: testing
  namespace: hierarchy
spec:
  displayName: Testing
  folderRef:
    external: '0000000000' # kpt-set: ${lz-folder-id}